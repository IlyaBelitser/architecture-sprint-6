apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    prometheus-monitored: "true"
spec:
  selector:
    app: scaletestapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
